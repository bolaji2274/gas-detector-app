name: gas_detector_app
description: IoT Gas Detection System Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  # firebase_core: ^2.24.2
  # firebase_auth: ^4.15.3
  # firebase_database: ^10.3.8
  # firebase_messaging: ^14.7.9
  firebase_core: ^4.3.0
  firebase_auth: ^6.1.3
  firebase_database: ^12.1.1
  firebase_messaging: ^16.1.0
  # State Management
  provider: ^6.1.1
  
  # UI Components
  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  fl_chart: ^0.65.0
  syncfusion_flutter_gauges: ^24.1.41
  flutter_spinkit: ^5.2.0
  intl: ^0.18.1
  
  # Notifications
  flutter_local_notifications: ^16.3.0
  
  # Utilities
  shared_preferences: ^2.2.2
  connectivity_plus: ^5.0.2
  url_launcher: ^6.2.2
  
  # Icons
  font_awesome_flutter: ^10.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # assets:
    # - assets/images/
    # - assets/icons/